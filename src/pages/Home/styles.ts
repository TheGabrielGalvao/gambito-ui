import { styled } from "../../util/stitches.config";

export const DashboardHeader = styled('div', {
    display: 'flex',
    justifyContent: "center",
    alignItems: "center",
    flexWrap: 'wrap',
    gap: '2rem',
    width: '100%',
    height: '35vh',
    padding: '2rem',
})

