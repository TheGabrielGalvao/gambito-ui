import { styled } from "../../util/stitches.config";

export const Content = styled('div', {
    position: 'absolute',
    display: "flex",
    flexDirection: "column",
    alignItems: 'center',
    top: 0,
    left: 88,
    height: '100vh',
    width: 'calc(100% - 88px)',
    transition: '$tran05',
    
})